<% content_for :title, "Reativar Funcionário" %>

<div class="container mt-5">
  <h1 class="text-center mb-4">Reativar Funcionário</h1>

  <div class="card shadow">
    <div class="card-body">
      <%= render "form", employee: @employee %>
    </div>
  </div>
</div>